---
phase: 01-spreadsheetparser-core
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - pyproject.toml
  - raganything/__init__.py
autonomous: true

must_haves:
  truths:
    - "pip install raganything[spreadsheet] installs openpyxl and xlrd"
    - "SpreadsheetParser and SpreadsheetConfig are importable from raganything top-level"
    - "pip install raganything (without extras) does NOT install openpyxl or xlrd"
  artifacts:
    - path: "pyproject.toml"
      provides: "spreadsheet optional dependency group"
      contains: "spreadsheet"
    - path: "raganything/__init__.py"
      provides: "SpreadsheetParser and SpreadsheetConfig re-exports"
      contains: "SpreadsheetParser"
  key_links:
    - from: "raganything/__init__.py"
      to: "raganything/spreadsheet.py"
      via: "import"
      pattern: "from \\.spreadsheet import"
    - from: "pyproject.toml"
      to: "raganything/spreadsheet.py"
      via: "optional dependency enables runtime imports"
      pattern: 'spreadsheet.*=.*openpyxl'
---

<objective>
Add the `spreadsheet` optional dependency group to pyproject.toml and export SpreadsheetParser/SpreadsheetConfig from the package's `__init__.py`.

Purpose: Makes the parser installable via `pip install raganything[spreadsheet]` and importable via `from raganything import SpreadsheetParser`.
Output: Updated pyproject.toml and __init__.py.
</objective>

<execution_context>
@/Users/alex/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alex/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-spreadsheetparser-core/01-RESEARCH.md
@pyproject.toml
@raganything/__init__.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add spreadsheet extras group and update __init__.py exports</name>
  <files>pyproject.toml, raganything/__init__.py</files>
  <action>
**pyproject.toml changes:**

1. Add `spreadsheet` group to `[project.optional-dependencies]` after `audio`:
```toml
spreadsheet = [
    "openpyxl>=3.1.2",
    "xlrd>=2.0.1",
]
```

2. Add both deps to the `all` group:
```toml
all = [
    ... existing deps ...,
    "openpyxl>=3.1.2",
    "xlrd>=2.0.1",
]
```

**raganything/__init__.py changes:**

Add SpreadsheetParser and SpreadsheetConfig imports following the same pattern as AudioProcessor:
```python
from .spreadsheet import (
    SpreadsheetParser as SpreadsheetParser,
    SpreadsheetConfig as SpreadsheetConfig,
)
```

Add `"SpreadsheetParser"` and `"SpreadsheetConfig"` to the `__all__` list.

IMPORTANT: The spreadsheet module uses guarded imports (openpyxl imported inside methods, not at module level), so importing SpreadsheetParser in __init__.py will NOT fail even if openpyxl is not installed. This matches the audio.py pattern.
  </action>
  <verify>
  1. `python -c "from raganything import SpreadsheetParser, SpreadsheetConfig; print('OK')"` succeeds
  2. `python -c "import tomllib; t = tomllib.load(open('pyproject.toml', 'rb')); assert 'spreadsheet' in t['project']['optional-dependencies']; assert 'openpyxl>=3.1.2' in t['project']['optional-dependencies']['spreadsheet']; print('OK')"` succeeds
  3. `python -c "import tomllib; t = tomllib.load(open('pyproject.toml', 'rb')); assert 'openpyxl>=3.1.2' in t['project']['optional-dependencies']['all']; print('OK')"` succeeds
  </verify>
  <done>spreadsheet extras group exists in pyproject.toml with openpyxl and xlrd. SpreadsheetParser and SpreadsheetConfig importable from raganything package. The `all` group includes both new dependencies.</done>
</task>

</tasks>

<verification>
1. `from raganything import SpreadsheetParser, SpreadsheetConfig` works
2. pyproject.toml has `spreadsheet = ["openpyxl>=3.1.2", "xlrd>=2.0.1"]`
3. `all` group includes both spreadsheet deps
4. `__all__` in __init__.py includes `"SpreadsheetParser"` and `"SpreadsheetConfig"`
</verification>

<success_criteria>
- `pip install raganything[spreadsheet]` would install openpyxl and xlrd
- SpreadsheetParser importable from top-level package
- No import errors when openpyxl is not installed (guarded imports)
</success_criteria>

<output>
After completion, create `.planning/phases/01-spreadsheetparser-core/01-03-SUMMARY.md`
</output>
