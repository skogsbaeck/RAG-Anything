---
phase: 03-routing-end-to-end
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - tests/test_e2e_spreadsheet.py
autonomous: true

must_haves:
  truths:
    - "process_document_complete() on an xlsx invokes the direct parser, not LibreOffice"
    - "A multi-sheet workbook produces multiple table content items with correct type, table_body, table_caption, and page_idx"
    - "When direct parsing fails, fallback logs WARNING and processing completes"
    - "Formula-None threshold triggers LibreOffice fallback when >10% cells are None"
  artifacts:
    - path: "tests/test_e2e_spreadsheet.py"
      provides: "E2E tests for spreadsheet pipeline"
      contains: "test_direct_parser_invoked"
  key_links:
    - from: "tests/test_e2e_spreadsheet.py"
      to: "raganything/processor.py"
      via: "mock-based integration testing ProcessorMixin.parse_document"
      pattern: "parse_document|parse_spreadsheet"
    - from: "tests/test_e2e_spreadsheet.py"
      to: "raganything/parser.py"
      via: "testing _none_ratio_exceeds_threshold and parse_spreadsheet fallback"
      pattern: "_none_ratio_exceeds_threshold|parse_spreadsheet"
---

<objective>
Create comprehensive E2E tests verifying the full spreadsheet pipeline: routing, contract, fallback, and threshold.

Purpose: Proves Phase 3 success criteria -- direct parser is invoked, content contract matches TableModalProcessor expectations, fallback works gracefully, and formula-None threshold triggers correctly.
Output: tests/test_e2e_spreadsheet.py with mock-based CI tests and one real integration test.
</objective>

<execution_context>
@/Users/alex/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alex/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-routing-end-to-end/03-01-SUMMARY.md
@raganything/parser.py
@raganything/processor.py
@raganything/spreadsheet.py
@raganything/modalprocessors.py
@tests/test_parse_spreadsheet.py
@tests/test_processor_spreadsheet.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create mock-based CI E2E tests</name>
  <files>tests/test_e2e_spreadsheet.py</files>
  <action>
Create tests/test_e2e_spreadsheet.py with programmatic openpyxl fixtures (NO binary files committed to git). Use `tmp_path` for all workbook files.

All fixtures should be created programmatically using openpyxl. Create a helper function `_create_workbook(tmp_path, sheets: dict[str, list[list]]) -> Path` that builds an xlsx file with named sheets from a dict mapping sheet names to 2D cell value lists.

Tests to write (all mock-based, fast, CI-safe):

1. **test_direct_parser_invoked** -- Create a simple 1-sheet xlsx. Call `MineruParser().parse_spreadsheet(path)`. Assert result is a list of dicts. Mock `parse_office_doc` on the parser instance and assert it was NOT called (proving direct path was taken). Verify result has at least one item with keys: type, table_body, table_caption, page_idx.

2. **test_content_contract_fields** -- Create a 1-sheet xlsx with known data (e.g., headers + 3 rows). Call parse_spreadsheet(). For each item in result, assert:
   - `item["type"] == "table"` (NOT "spreadsheet_table")
   - `isinstance(item["table_body"], str)` and it contains pipe characters (markdown table)
   - `isinstance(item["table_caption"], list)` and len > 0
   - `isinstance(item["page_idx"], int)`

3. **test_multi_sheet_produces_multiple_items** -- Create a 3-sheet xlsx (each non-empty). Call parse_spreadsheet(). Assert `len(result) >= 3`. Assert page_idx values are 0, 1, 2 (preserving workbook tab order). Assert each item's table_caption contains the sheet name.

4. **test_fallback_on_parse_failure** -- Create an xlsx. Patch `SpreadsheetParser.parse` (inside raganything.parser module) to raise `ValueError("test error")`. Also patch `MineruParser.parse_office_doc` to return a known sentinel list `[{"type": "text", "text": "fallback"}]`. Call `MineruParser().parse_spreadsheet(path)`. Assert result equals the sentinel. Assert parse_office_doc was called once. Use `caplog` to assert a WARNING log message containing the filename and "falling back".

5. **test_fallback_logs_warning** -- Same setup as test 4, but specifically assert `caplog` contains a record at WARNING level with the filename.

6. **test_formula_none_threshold_triggers_fallback** -- Import `_none_ratio_exceeds_threshold` from `raganything.parser`. Test with a content_list containing one table item where >10% of cells are empty. Assert returns True. Test with a content_list where <10% cells are empty. Assert returns False.

7. **test_formula_none_threshold_in_parse_spreadsheet** -- Create an xlsx. Patch `SpreadsheetParser.parse` to return a content_list with many empty cells (>10% None ratio). Patch `MineruParser.parse_office_doc` to return a sentinel. Call parse_spreadsheet(). Assert parse_office_doc WAS called (threshold triggered fallback). Use `caplog` to assert WARNING about "None cells".

8. **test_importerror_propagates** -- Patch `raganything.parser` so that the `from raganything.spreadsheet import ...` inside parse_spreadsheet raises ImportError. Call parse_spreadsheet(). Assert ImportError is raised (not swallowed). Assert parse_office_doc was NOT called.

Group related assertions to avoid test proliferation. Tests 4 and 5 can be combined if natural.

Use `unittest.mock.patch` for mocking. Use `@pytest.fixture` for common xlsx fixture creation.

IMPORTANT: The type MUST be "table" (not "spreadsheet_table") -- this is locked per user decisions. Tests must assert this explicitly.

IMPORTANT: Use `git add -f` when committing the test file due to .gitignore `test_*` pattern.
  </action>
  <verify>
Run: `python -m pytest tests/test_e2e_spreadsheet.py -v` -- all tests pass.
Run: `python -m pytest tests/ -v` -- all tests pass including existing ones.
  </verify>
  <done>
tests/test_e2e_spreadsheet.py exists with 6-8 tests covering: direct parser invocation, content contract fields, multi-sheet handling, fallback on failure, WARNING logging, formula-None threshold unit test, threshold-triggered fallback integration, ImportError propagation. All tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add optional real integration test</name>
  <files>tests/test_e2e_spreadsheet.py</files>
  <action>
Add one additional test to tests/test_e2e_spreadsheet.py marked with `@pytest.mark.slow`:

**test_real_lightrag_integration** -- This test verifies data flows through to LightRAG. Mark with `@pytest.mark.slow` so it is skipped in normal CI runs.

```python
@pytest.mark.slow
async def test_real_lightrag_integration(tmp_path):
```

Implementation:
1. Create an xlsx with known cell values (e.g., "Revenue: 42000", "Department: Engineering").
2. Try to import RAGAnything and LightRAG. If import fails, `pytest.skip("RAGAnything/LightRAG not fully installed")`.
3. Initialize a RAGAnything instance with a temporary working directory (tmp_path / "rag_workdir"). Use minimal config -- no LLM needed for insertion, just the parsing + text insertion path.
4. Call `await rag.process_document_complete(str(xlsx_path))`.
5. Assert no exception raised.
6. Query LightRAG for known values (e.g., search for "Revenue" or "Engineering") and assert results contain the known cell values.

If full LightRAG initialization is too complex without an LLM key, simplify to:
1. Create xlsx, call parse_spreadsheet() to get content_list.
2. Assert content_list is non-empty.
3. Assert known cell values ("42000", "Engineering") appear in at least one table_body string.
4. This proves data survives the full parsing pipeline end-to-end.

The test should be self-contained and not require any external services or API keys.
  </action>
  <verify>
Run: `python -m pytest tests/test_e2e_spreadsheet.py -v -m "not slow"` -- all fast tests pass, slow test is skipped.
Run: `python -m pytest tests/test_e2e_spreadsheet.py -v -m slow` -- slow test runs and passes (or skips if deps missing).
  </verify>
  <done>
test_real_lightrag_integration exists with @pytest.mark.slow. It is skipped in normal test runs. When run with -m slow, it verifies known spreadsheet values survive the parsing pipeline.
  </done>
</task>

</tasks>

<verification>
1. `python -m pytest tests/test_e2e_spreadsheet.py -v` -- all non-slow tests pass
2. `python -m pytest tests/ -v -m "not slow"` -- full test suite passes
3. Tests explicitly assert `type == "table"` (not "spreadsheet_table")
4. Tests assert direct parser path taken (parse_office_doc not called)
5. Tests assert fallback triggers with WARNING when direct parse fails
6. Tests assert formula-None threshold triggers fallback
</verification>

<success_criteria>
- 6-8 mock-based tests covering routing, contract, fallback, threshold
- 1 slow integration test proving data survives full pipeline
- All tests use programmatic openpyxl fixtures (no binary files)
- All fast tests pass in CI
- Phase 3 success criteria (from ROADMAP.md) are provably met by test assertions
</success_criteria>

<output>
After completion, create `.planning/phases/03-routing-end-to-end/03-02-SUMMARY.md`
</output>
